{
  "name": "webapp",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "build:upload": "tsc src/upload-server.ts --outDir dist --module Node16 --target es2022 --moduleResolution node16 --esModuleInterop --skipLibCheck",
    "preview": "wrangler pages dev",
    "deploy": "npm run build && wrangler pages deploy",
    "deploy:upload": "npm run build:upload && docker build -t eltigre-upload -f Dockerfile.upload . && docker compose -f docker-compose.upload.yml up -d",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "@hono/node-server": "^1.19.17",
    "hono": "^4.12.31"
  },
  "devDependencies": {
    "@hono/vite-build": "^1.11.1",
    "@hono/vite-dev-server": "^0.26.1",
    "@types/express": "^5.0.6",
    "@types/node": "^26.2.0",
    "typescript": "^5.9.3",
    "vite": "^8.1.4",
    "wrangler": "^4.110.0"
  }
}
